# Next Laptop Store Project

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Structure

- `app/page.tsx`: Trang chủ mobile-first dựng từ `trang_ch_laptop_store/code.html`
- `app/layout.tsx`: Layout gốc của Next.js App Router
- `app/globals.css`: Reset tối thiểu
